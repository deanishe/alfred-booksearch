
magefile.go
magefile_*.go {
    prep: "
        # verifying magefile
        mage -l
    "
}

modd.conf
**/*.go
!mage*.go
!vendor/** {
    prep: "
        # run unit tests
        go test -v @dirmods \
        && mage -v run
    "
}
